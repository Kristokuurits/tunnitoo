<!DOCTYPE html>
<html>
<head>
    <title>Jagamine</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Jagamine</h1>
        <form method="GET">
            <div class="form-group">
                <label for="number1">Esimene arv:</label>
                <input type="number" class="form-control" id="number1" name="number1">
            </div>
            <div class="form-group">
                <label for="number2">Teine arv:</label>
                <input type="number" class="form-control" id="number2" name="number2">
            </div>
            <button type="submit" class="btn btn-primary">Jaga</button>
        </form>
        <br>
        <?php
        if (!empty($_GET['number1']) && !empty($_GET['number2'])) {
            $number1 = $_GET['number1'];
            $number2 = $_GET['number2'];

            if ($number2 != 0) {
                $result = $number1 / $number2;
                echo "Tulemus: " . $result;
            } else {
                echo "Viga: Ei saa jagada nulliga!";
            }
        }
        ?>
    </div>
</body>
</html>

<!DOCTYPE html>
<html>
<head>
    <title>Vanus</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Vanus</h1>
        <form method="GET">
            <div class="form-group">
                <label for="age1">Esimese inimese vanus:</label>
                <input type="number" class="form-control" id="age1" name="age1">
            </div>
            <div class="form-group">
                <label for="age2">Teise inimese vanus:</label>
                <input type="number" class="form-control" id="age2" name="age2">
            </div>
            <button type="submit" class="btn btn-primary">Kontrolli</button>
        </form>
        <br>
        <?php
        if (!empty($_GET['age1']) && !empty($_GET['age2'])) {
            $age1 = $_GET['age1'];
            $age2 = $_GET['age2'];

            if ($age1 == $age2) {
                echo "Mõlemad inimesed on ühevanused.";
            } elseif ($age1 > $age2) {
                echo "Esimese inimese vanus on suurem.";
            } else {
                echo "Teise inimese vanus on suurem.";
            }
        }
        ?>
    </div>
</body>
</html>

<!DOCTYPE html>
<html>
<head>
    <title>Ristkülik või ruut</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Ristkülik või ruut</h1>
        <form method="GET">
            <div class="form-group">
                <label for="side1">Esimese külje pikkus:</label>
                <input type="number" class="form-control" id="side1" name="side1">
            </div>
            <div class="form-group">
                <label for="side2">Teise külje pikkus:</label>
                <input type="number" class="form-control" id="side2" name="side2">
            </div>
            <button type="submit" class="btn btn-primary">Kontrolli</button>
        </form>
        <br>
        <?php
        if (!empty($_GET['side1']) && !empty($_GET['side2'])) {
            $side1 = $_GET['side1'];
            $side2 = $_GET['side2'];

            if ($side1 == $side2) {
                echo "Sisestatud küljepikkused moodustavad ruudu.";
            } else {
                echo "Sisestatud külje pikkused moodustavad ristküliku.";
            }
        }
        ?>
    </div>
</body>
</html>

<!DOCTYPE html>
<html>
<head>
    <title>Ristkülik või ruut II</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Ristkülik või ruut II</h1>
        <form method="GET">
            <div class="form-group">
                <label for="side1">Esimese külje pikkus:</label>
                <input type="number" class="form-control" id="side1" name="side1">
            </div>
            <div class="form-group">
                <label for="side2">Teise külje pikkus:</label>
                <input type="number" class="form-control" id="side2" name="side2">
            </div>
            <button type="submit" class="btn btn-primary">Kontrolli</button>
        </form>
        <br>
        <?php
        if (!empty($_GET['side1']) && !empty($_GET['side2'])) {
            $side1 = $_GET['side1'];
            $side2 = $_GET['side2'];

            if ($side1 == $side2) {
                echo "Sisestatud küljepikkused moodustavad ruudu.";
            } else {
                echo "Sisestatud küljepikkused moodustavad ristküliku.";
            }
        } else {
            echo "Palun sisestage küljepikkused.";
        }
        ?>
    </div>
</body>
</html>

<!DOCTYPE html>
<html>
<head>
    <title>Juubel</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Juubel</h1>
        <form method="GET">
            <div class="form-group">
                <label for="birthyear">Sünniaasta:</label>
                <input type="number" class="form-control" id="birthyear" name="birthyear">
            </div>
            <button type="submit" class="btn btn-primary">Kontrolli</button>
        </form>
        <br>
        <?php
        if (!empty($_GET['birthyear'])) {
            $birthyear = $_GET['birthyear'];

            if (($birthyear % 100 == 0 && $birthyear % 400 == 0) || ($birthyear % 100 != 0 && $birthyear % 4 == 0)) {
                echo $birthyear . " on juubeliaasta.";
            } else {
                echo $birthyear . " ei ole juubeliaasta.";
            }
        }
        ?>
    </div>
</body>
</html>

<!DOCTYPE html>
<html>
<head>
    <title>Hinne</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css">
</head>
<body>
    <div class="container">
        <h1>Hinne</h1>
        <form method="GET">
            <div class="form-group">
                <label for="points">KT punktid:</label>
                <input type="number" class="form-control" id="points" name="points">
            </div>
            <button type="submit" class="btn btn-primary">Kontrolli</button>
        </form>
        <br>
        <?php
        if (!empty($_GET['points'])) {
            $points = $_GET['points'];

            if ($points > 10) {
                echo "Imeline!";
            } elseif ($points >= 5 && $points <= 9) {
                echo "OK!";
            } elseif ($points < 5) {
                echo "YES SIR!";
            }
        } else {
            echo "SISESTA OMA PUNKTID!";
        }
        ?>
    </div>
</body>
</html>